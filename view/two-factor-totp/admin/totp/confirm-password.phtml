<?php
/**
 * The password prompt that stands in front of passkey management.
 *
 * Rendered by TotpController::requirePasswordConfirmation() in place of the
 * page that was asked for, and posting back to that same page.
 *
 * @var \Omeka\Entity\User $user
 * @var \TwoFactorTotp\Form\PasswordConfirmForm $form
 */
$translate = $this->plugin('translate');
$this->htmlElement('body')->appendAttribute('class', 'two-factor-totp confirm-password');
?>
<?php echo $this->pageTitle($translate('Passkeys'), 1, $translate('Users')); ?>

<p><?php echo $translate('Confirm with your password to continue.'); ?></p>

<?php echo $this->form($form); ?>
